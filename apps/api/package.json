{
  "name": "@repo/pennapi",
  "scripts": {
    "build": "cargo build --release",
    "dev": "RUSTUP_TOOLCHAIN=nightly bacon clippy",
    "test": "cargo test",
    "lint": "cargo clippy",
    "format": "cargo fmt"
  }
}
