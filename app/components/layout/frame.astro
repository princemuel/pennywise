---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"div">;

const { class: cls } = Astro.props;
---

<div class:list={["mx-auto w-full max-w-(--breakpoint-lg) px-4 sm:px-6 lg:px-8", cls]}>
  <slot />
</div>
